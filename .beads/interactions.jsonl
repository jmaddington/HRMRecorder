{"id":"int-591390af","kind":"field_change","created_at":"2026-05-19T02:56:23.422841Z","actor":"Jonathan Addington","issue_id":"HRMRecorder-bri","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Moved session list off the main screen into a new SessionsView pushed via a 'Sessions' NavigationLink. Main screen now only shows live BPM + device + recording (no longer queries the DB). SessionsView owns the ForEach(sessions) list, per-session swipe delete + CSV, Export-All, Delete-All, and the DB-path footer; loads via model.db.sessions() on appear. ShareSheet/alert plumbing moved with it. Pure presentation refactor, no DB/HeartRateManager changes. Verified: xcodebuild iphonesimulator BUILD SUCCEEDED. Version bumped 0.11->0.12, build 6->7."}}
{"id":"int-e4f23662","kind":"field_change","created_at":"2026-05-19T11:11:00.687378Z","actor":"Jonathan Addington","issue_id":"HRMRecorder-hhe","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Reordered ContentView so recordSection (Start/Stop, Elapsed, Samples) sits above deviceSection: liveSection -> recordSection -> deviceSection -> Sessions link. Collapsed the 5-row device section into a DisclosureGroup (collapsed by default) whose label is the 'Strap: <name>' summary (connected name visible at a glance); expanding reveals Sensor, Firmware, Change/Choose Device, Forget Device. Preserved semantics: Change/Choose & Forget stay .disabled(hr.isRecording); Choose vs Change label logic; Forget shown only when connected or preferredDeviceUUID exists; showDevicePicker sheet flow unchanged. Pure presentation, no HeartRateManager/DB changes. Verified: xcodebuild iphonesimulator BUILD SUCCEEDED. Version bumped 0.12->0.13, build 7->8."}}
{"id":"int-cfee3153","kind":"field_change","created_at":"2026-05-19T11:16:27.9333Z","actor":"Jonathan Addington","issue_id":"HRMRecorder-r1g","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Added optional date-gated CSV export. HRDatabase.exportCSV now takes from/to Date? params (default nil = full history, so existing per-session swipe export and Export-All are unchanged); builds the WHERE clause dynamically with s.ts >= from AND s.ts < to bound parameters, leveraging idx_samples_ts. Streaming/chunked 64KB write path and queue.sync split unchanged. A date-filtered range matching zero samples writes no file and returns nil (graceful empty-range). SessionsView gains a 'Limit to date range' toggle revealing From/To .date DatePickers; bounds are whole calendar days (startOfDay..<startOfDay+1d, end-day inclusive); start>end disables the export button with an inline warning. Verified: xcodebuild iphonesimulator BUILD SUCCEEDED. Per corrected release policy: MARKETING_VERSION 0.13->0.14, CURRENT_PROJECT_VERSION reset 8->1."}}
{"id":"int-3bcb8414","kind":"field_change","created_at":"2026-05-19T11:33:51.766806Z","actor":"Jonathan Addington","issue_id":"HRMRecorder-5a2","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Added centered version footer to ContentView; build bumped to 0.14 (3); Release archive succeeded"}}
{"id":"int-398e5925","kind":"field_change","created_at":"2026-05-19T11:40:17.115133Z","actor":"Jonathan Addington","issue_id":"HRMRecorder-km4.1","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Phase 0 done, no behavior change. Added devices(id TEXT pk=UUID,name,manufacturer,model,firmware,body_location,first_seen) + nullable samples.device_id (columnExists/ALTER migration) + idx_samples_device, in HRDatabase.init. insertStmt extended to 7 cols; insertSample gains deviceID:String?=nil (default keeps all call sites + single-strap output byte-identical; still one fire-and-forget queue.async, +1 bind). New setDevice() INSERT OR IGNORE + COALESCE UPDATE mirroring setSessionDevice. exportCSV: LEFT JOIN devices d, COALESCE(d.*,e.*) for legacy 12 cols (NULL device_id -> identical to pre-feature output), appended device_id+device_strap_name; header+row updated. README schema/CSV docs updated. xcodebuild iphonesimulator BUILD SUCCEEDED. Version 0.14->0.15, build reset 3->1."}}
{"id":"int-260cd5e0","kind":"field_change","created_at":"2026-05-19T11:46:18.870143Z","actor":"Jonathan Addington","issue_id":"HRMRecorder-km4.2","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Phase 1 done — BLE multi-peripheral refactor with primary-mirror facade; single-strap behavior unchanged. Added ConnectedDevice value type + @Published devices:[UUID:ConnectedDevice] + primaryDeviceID; scalar @Published props now mirror the primary device via refreshPrimaryMirror() called on every devices mutation. preferredDeviceUUID -> preferredDeviceIDs set (computed prop migrates the legacy single key once). autoConnectPreferred Bool -> Set<UUID>. willRestoreState restores ALL peripherals (fixes the latent .first bug). autoConnectOrScan reconnects all remembered (retrievePeripherals/retrieveConnectedPeripherals/scan per pending UUID). didDisconnect issues a pending reconnect for any tracked/remembered strap (per-strap background resilience — the 'backup strap' guarantee). connect/didConnect/discover/didUpdateValueFor route by peripheral.identifier; ingest(_:from:) updates the source device's entry, mirrors primary, LiveActivity.update gated to primary. select(): not-recording = switch (single-select picker, P3 adds explicit add-another); recording = add into same session. insertSample still called WITHOUT deviceID (P2 adds attribution) so single-strap CSV byte-identical. xcodebuild iphonesimulator BUILD SUCCEEDED. Version 0.15->0.16, build stays 1."}}
{"id":"int-61d8b68a","kind":"field_change","created_at":"2026-05-19T11:47:33.621873Z","actor":"Jonathan Addington","issue_id":"HRMRecorder-km4.3","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Phase 2 done — per-sample device attribution. ingest(_:from:) now passes deviceID: peripheral UUID to db.insertSample, so each sample is attributed to its source strap in one shared session (sessionID/activeSessionID/resume unchanged; sessionSampleCount stays one aggregate). persistDeviceInfo() now also registers every connected strap's identity into the devices table via db.setDevice (idempotent COALESCE accumulation of late DIS reads), and still writes the primary strap onto the session row for the unchanged session-level label. Single-strap CSV byte-identical: export COALESCEs the devices row over the session row to the same identity for the legacy 12 columns; device_id/device_strap_name (appended cols) are now populated. xcodebuild iphonesimulator BUILD SUCCEEDED. Version 0.16->0.17, build stays 1."}}
{"id":"int-040bbbae","kind":"field_change","created_at":"2026-05-19T11:51:10.805963Z","actor":"Jonathan Addington","issue_id":"HRMRecorder-km4.4","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Phase 3 done — minimal multi-strap UI. liveSection unchanged (primary BPM = liveness check). New otherStrapsSection: one compact line per non-primary strap (small BPM + name + status dot), shown only when devices.count>1 so the single-strap screen is unchanged. DevicePickerView is now additive/non-dismissing: connected straps show a green checkmark, tapping a connected one disconnects it (hr.forget), tapping a new one adds it (no auto-dismiss), title becomes 'Straps', footer rewritten for multi-strap + per-strap single-connection caveat, Cancel->Done. deviceSection: 'Add / Change Strap' vs 'Choose Device', 'Forget All Straps', strapSummary ('N straps'); Add/Change no longer disabled while recording (enables backup/swap mid-session = px8). HeartRateManager: select() now additive; new forget(_:), secondaryDevices, connectedDeviceIDs, hasDevice, hasRememberedStrap; maybePresentPicker uses hasRememberedStrap (not stale UserDefaults key). xcodebuild iphonesimulator BUILD SUCCEEDED. Version 0.17->0.18, build 1."}}
